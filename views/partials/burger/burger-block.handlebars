<li>
	{{burger_name}}

{{#if devour }}
	<button class="btn-success" data-id="{{id}}"    data-devoured="{{devour}}" data-newsleep="{{sleep}}">
		{{#if devour}}Devoured! {{/if}}
	</button>

	{{/if}}
</li>
